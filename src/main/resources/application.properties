spring.application.name=pattern
# ???URL???pattern_db????????????????????
# 数据库配置：优先使用环境变量，如果没有则使用默认值（本地开发用）
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/pattern_db?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:Aa147369852}

# MySQL8+???????????????????
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA???????????SQL?
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# File Upload Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
spring.servlet.multipart.enabled=true

# Upload Directory
file.upload-dir=uploads/patterns

# Static Resource Configuration
spring.web.resources.add-mappings=true
spring.web.resources.cache.cachecontrol.max-age=31536000
spring.web.resources.cache.cachecontrol.cache-public=true

# Ensure fonts are served with correct MIME types
spring.mvc.contentnegotiation.favor-path-extension=false